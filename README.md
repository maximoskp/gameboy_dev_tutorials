# gameboy_dev_tutorials
An attempt to create a tutorial for myself that starts with C and hopefully ends with GBZ80 assembly.

When downloading a new version on mac, to make it trust it run

```
xattr -d com.apple.quarantine *
```

in `gbdk/lib` and `gbdk/libexec/sdcc`, or wherever there are executables.